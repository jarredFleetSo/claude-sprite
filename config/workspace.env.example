# =============================================================================
# Remote Claude Code Workspace — Configuration
# =============================================================================
# Copy this file to config/workspace.env and fill in the required values.
#
#   cp config/workspace.env.example config/workspace.env
#
# Values marked [REQUIRED] must be set before running bootstrap.sh.
# All other values have sensible defaults.
# =============================================================================

# -----------------------------------------------------------------------------
# Workspace identity
# -----------------------------------------------------------------------------

# Human-readable name for this workspace (used in labels / prompts)
WORKSPACE_NAME="claude-workspace"

# The non-root user that owns the workspace
WORKSPACE_USER="coder"

# Root directory for project files
WORKSPACE_DIR="/home/${WORKSPACE_USER}/workspace"

# -----------------------------------------------------------------------------
# Project repository
# -----------------------------------------------------------------------------

# Git clone URL for the project to check out during bootstrap (optional)
# Example: git@github.com:your-org/your-repo.git
PROJECT_REPO_URL=""

# -----------------------------------------------------------------------------
# Service ports
# -----------------------------------------------------------------------------

# code-server (VS Code in the browser)
CODE_SERVER_PORT=8080

# Authentication mode for code-server.
# Set to "none" because Cloudflare Access handles authentication upstream.
CODE_SERVER_AUTH="none"

# ttyd (terminal in the browser)
TTYD_PORT=7681

# Application preview port (e.g., Vite / Next.js dev server)
PREVIEW_PORT=3000

# -----------------------------------------------------------------------------
# Cloudflare Tunnel  [REQUIRED]
# -----------------------------------------------------------------------------

# The connector token obtained from the Cloudflare Zero Trust dashboard.
# Create a tunnel at: https://one.dash.cloudflare.com → Networks → Tunnels
CLOUDFLARE_TUNNEL_TOKEN=""

# Your Cloudflare-managed base domain (e.g., "example.com")
CLOUDFLARE_DOMAIN=""

# Per-service hostnames — override if you need a different naming scheme
CODE_HOSTNAME="code.${CLOUDFLARE_DOMAIN}"
TERM_HOSTNAME="term.${CLOUDFLARE_DOMAIN}"
PREVIEW_HOSTNAME="preview.${CLOUDFLARE_DOMAIN}"

# -----------------------------------------------------------------------------
# Git identity (optional — prompted interactively if empty)
# -----------------------------------------------------------------------------

GIT_USER_NAME=""
GIT_USER_EMAIL=""

# -----------------------------------------------------------------------------
# Optional features
# -----------------------------------------------------------------------------

# Set to "true" to install Docker (docker.io) during bootstrap
ENABLE_DOCKER="false"

# Set to "true" to register the preview-port service with cloudflared
ENABLE_PREVIEW_SERVICE="false"

# -----------------------------------------------------------------------------
# tmux
# -----------------------------------------------------------------------------

# Name of the persistent tmux session created at bootstrap
TMUX_SESSION_NAME="workspace"
